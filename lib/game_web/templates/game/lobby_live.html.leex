<h1>Room: <%= @room %></h1>
<% len = length(@state.id_list) %>
<%= if len > 2 do %>
  <button phx-click="start">START GAME</button>
<% else %>
  <h2>You need <%= 3 - len %> more player(s) to start</h2>
<% end %>

<ul>
<%= for {_, name} <- @state.id_list do %>
  <li><%= name %></li>
<% end %>
</ul>

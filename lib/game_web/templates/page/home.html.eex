<h1>Just One</h1>

<h2>Welcome, <%= @name %> <sup>(<a href="<%= Routes.page_path(@conn, :name) %>">change</a>)</sup></h2>

<div>
  <a href="<%= Routes.page_path(@conn, :new) %>"><button>NEW GAME</button></a>
  <a href="<%= Routes.page_path(@conn, :join) %>"><button>JOIN GAME</button></a>
</div>
